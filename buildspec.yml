version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install -g pnpm
  build:
    commands:
      - pnpm install
      - pnpm build

artifacts:
  files:
    - '**/*'
  name: chatter-build-artifacts
